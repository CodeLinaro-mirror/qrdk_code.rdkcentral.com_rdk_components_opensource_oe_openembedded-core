
# Pulled from OpenEmbedded
# 
# Commented by: Saul Wold <saul.wold@intel.com>

Upstream-Status: Inappropriate [licensing]

Index: gettext-0.16.1/gettext-runtime/gnulib-lib/error.h
===================================================================
--- gettext-0.16.1.orig/gettext-runtime/gnulib-lib/error.h
+++ gettext-0.16.1/gettext-runtime/gnulib-lib/error.h
@@ -50,14 +50,14 @@ extern void error_at_line (int __status,
 /* If NULL, error will flush stdout, then print on stderr the program
    name, a colon and a space.  Otherwise, error will call this
    function without parameters instead.  */
-extern DLL_VARIABLE void (*error_print_progname) (void);
+extern void (*error_print_progname) (void);
 
 /* This variable is incremented each time `error' is called.  */
-extern DLL_VARIABLE unsigned int error_message_count;
+extern unsigned int error_message_count;
 
 /* Sometimes we want to have at most one error per line.  This
    variable controls whether this mode is selected or not.  */
-extern DLL_VARIABLE int error_one_per_line;
+extern int error_one_per_line;
 
 #ifdef	__cplusplus
 }
Index: gettext-0.16.1/gettext-tools/gnulib-lib/error.h
===================================================================
--- gettext-0.16.1.orig/gettext-tools/gnulib-lib/error.h
+++ gettext-0.16.1/gettext-tools/gnulib-lib/error.h
@@ -50,14 +50,14 @@ extern void error_at_line (int __status,
 /* If NULL, error will flush stdout, then print on stderr the program
    name, a colon and a space.  Otherwise, error will call this
    function without parameters instead.  */
-extern DLL_VARIABLE void (*error_print_progname) (void);
+extern void (*error_print_progname) (void);
 
 /* This variable is incremented each time `error' is called.  */
-extern DLL_VARIABLE unsigned int error_message_count;
+extern unsigned int error_message_count;
 
 /* Sometimes we want to have at most one error per line.  This
    variable controls whether this mode is selected or not.  */
-extern DLL_VARIABLE int error_one_per_line;
+extern int error_one_per_line;
 
 #ifdef	__cplusplus
 }
Index: gettext-0.16.1/gettext-tools/libgettextpo/error.h
===================================================================
--- gettext-0.16.1.orig/gettext-tools/libgettextpo/error.h
+++ gettext-0.16.1/gettext-tools/libgettextpo/error.h
@@ -50,14 +50,14 @@ extern void error_at_line (int __status,
 /* If NULL, error will flush stdout, then print on stderr the program
    name, a colon and a space.  Otherwise, error will call this
    function without parameters instead.  */
-extern DLL_VARIABLE void (*error_print_progname) (void);
+extern void (*error_print_progname) (void);
 
 /* This variable is incremented each time `error' is called.  */
-extern DLL_VARIABLE unsigned int error_message_count;
+extern unsigned int error_message_count;
 
 /* Sometimes we want to have at most one error per line.  This
    variable controls whether this mode is selected or not.  */
-extern DLL_VARIABLE int error_one_per_line;
+extern int error_one_per_line;
 
 #ifdef	__cplusplus
 }
